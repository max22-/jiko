WHITESPACE = _{ " " }
integer = @{ "-"? ~ ASCII_DIGIT+ }
boolean = @{ "true" | "false" }
word = @{ !boolean ~ ASCII_ALPHA+ }
quotation = { "[" ~ program* ~ "]" }
program = { integer | boolean | word | quotation }
input = { SOI ~ program* ~ EOI }